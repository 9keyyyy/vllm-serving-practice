global:
  scrape_interval: 15s

scrape_configs:
  # FastAPI 메트릭 수집
  - job_name: 'llm-api'
    static_configs:
      - targets: ['api:8080']
    metrics_path: /metrics

  # Prometheus 자체 모니터링
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']